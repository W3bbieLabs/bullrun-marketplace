import Link from "next/link";

const Navbar = () => {
  return (
    <nav className="border-top border-b border-black py-16">
      <div className="flex justify-between items-center px-4">
        {/* Logo */}
        {/* Hamburger menu */}
      </div>

      {/* Mobile menu */}
    </nav>
  );
};

export default Navbar;
